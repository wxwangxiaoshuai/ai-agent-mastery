# 课程优化落地计划

> 制定日期：2026-07-27
> 依据：`curriculum-review-report.md`（v2）
> 覆盖范围：审查报告中全部 9 项问题 + M17–M19 新模块建设
> 总工作量估算：**约 24–30 人日**（内容编写占七成）

---

## 总览

计划分 10 个工作波次（W0–W9）。排序遵循三条原则：**先止血再治病**（数据一致性问题成本极低但影响可信度，优先做完）、**先立防线再施工**（校验脚本放在第二位，之后所有改动自动受保护）、**先基建再内容**（组件、别名层、渲染能力先就位，内容编写才不会返工）。

| 波次 | 名称 | 解决的问题 | 工作量 | 依赖 |
|------|------|-----------|--------|------|
| W0 | 修数据一致性与文档漂移 | 问题 2、5、6（部分） | 0.5 天 | — |
| W1 | 一致性校验脚本 + CI | 全部问题的防回归 | 1 天 | W0 |
| W2 | 补齐交互组件覆盖 | 问题 1 | 4 天 | W1 |
| W3 | 模型别名层 | 问题 6 | 1.5 天 | W1 |
| W4 | 收敛语言定位 | 问题 3 | 0.5 天 | W1 |
| W5 | 补练习闭环 | 问题 4 | 5 天 | W1 |
| W6 | 扩写偏薄课程 + 阶段调整 | 问题 7、8 | 2 天 | W3、W4 |
| W7 | M17–M19 数据层骨架 | 问题 9 | 0.5 天 | W0 |
| W8 | M17 内容编写 | 问题 9 | 5 天 | W7、W2 |
| W9 | M18/M19 内容编写 | 问题 9 | 6 天 | W8 |

W2 / W3 / W4 / W5 之间无依赖，可并行。W8 必须在 W9 之前——M18-03 产出的 PRD 直接喂给 M17 的 Spec-Driven 流程，P19 以 P18 的产品为对象。

---

## W0 · 修数据一致性与文档漂移

**目标**：不动一个字的正文，把所有"数字对不上"和"文档说了但代码里没有"的问题清掉。这是投入产出比最高的一波。

### 任务清单

**T0.1 · `src/data/types.ts`**

- 给 `Project` 接口增加 `hours: number` 字段（项目预计实践时长）
- 从 `LessonType` 联合类型中删除 `'项目'`——定义了但 0 处使用，属于死定义

**T0.2 · `src/data/curriculum.ts`**

- 为 16 个 `project` 对象补 `hours` 值，取值使得每个模块满足：
  ```
  module.hours === Math.round(Σ lesson.duration / 60) + project.hours
  ```
  参考基线（现声明值 − lesson 合计）：M1 需补约 5.6h、M13 需补约 10.2h、M16 需补约 5.5h，其余在 6–8h 之间。建议顺势复核各项目的实际工作量是否配得上这个数字，而不是机械倒推。

**T0.3 · 页面展示（5 处）**

改为分列显示，让学员看得懂时长构成：

| 文件 | 行 | 现状 | 改为 |
|------|-----|------|------|
| `src/pages/CurriculumPage.tsx` | 27 | `{module.hours} 小时` | `精讲 {lessonH}h · 实战 {projectH}h` |
| `src/pages/ModulePage.tsx` | 52 | `⏱ {module.hours}h · {totalMin}m 课程` | 同上 |
| `src/pages/LessonPage.tsx` | 284 | `{module.hours}h` | 保留总时长即可 |
| `src/pages/ProjectPage.tsx` | 211 | `{mod.hours}h` | 改为显示 `project.hours` |
| `src/pages/RoadmapPage.tsx` | 69 | `⏱ {m.hours}h` | 保留总时长 |

建议在 `src/data/curriculum.ts` 导出一个 `getModuleHours(m)` 辅助函数，五处统一调用，避免各页面各算各的。

**T0.4 · `README.md`**

- `~159 小时` → 与 curriculum.ts 计算值一致（W7 之后再更新为含新模块的 208h）
- 模块/课程/项目数量在 W7 完成后一并更新为 19/113/19

**T0.5 · `CLAUDE.md`**

- 交互组件表中**删除 `chunkingDemo` / `ragPipeline` 两行**——这两个组件不存在，会持续误导后续开发（包括 AI 协作者按不存在的组件写内容）。W2 实现后再加回来。
- 语言约定一行留到 W4 处理

**T0.6 · 模型标识笔误**

`claude-opus-4-8` 不是任何真实模型标识，5 处：

```
src/content/module-01/lesson-l01-04.md:124
src/content/module-01/lesson-l01-04.md:145
src/content/module-15/lesson-l15-02.md:66
src/content/module-15/lesson-l15-02.md:88
src/content/module-15/lesson-l15-02.md:89
```

W0 先改成当前有效的标识止血，W3 再统一换成语义别名。

### 验收标准

- `pnpm build` 通过
- 任取 3 个模块页，显示的时长构成与数据层一致
- `grep -rn "claude-opus-4-8" src/` 无结果
- `grep -n "chunkingDemo\|ragPipeline" CLAUDE.md` 无结果

---

## W1 · 一致性校验脚本 + CI（防回归的地基）

**目标**：把这次审查发现的每一类问题都变成一条自动化检查。**没有这一步，W0 修好的东西下个月会重新腐烂**——这次审查发现的九个问题里，至少五个是随时间自然漂移出来的。

### 新建 `scripts/check-curriculum.mjs`

| 检查项 | 规则 | 对应问题 |
|--------|------|----------|
| C1 时长一致性 | 每个模块 `hours === Σlesson.duration/60 + project.hours`（允许 ±0.5h 取整误差） | 问题 2 |
| C2 组件引用有效 | 内容中每个 `::interactive{type="X"}` 的 X 必须在 `componentMap` 中，且对应 `.tsx` 文件存在 | 问题 1 |
| C3 组件覆盖率 | 每个模块至少含 1 个 `::interactive` | 问题 1 |
| C4 反向孤儿检查 | `componentMap` 中每个组件至少被内容引用 1 次（防止再出现 EmbeddingExplorer 那种"写好了没接"） | 问题 1 |
| C5 模型标识白名单 | 内容中出现的模型标识必须在 `src/data/models.ts` 的白名单内 | 问题 6 |
| C6 结构完整性 | 每个 lesson 含"要点总结"章节 | — |
| C7 练习覆盖 | 每个 lesson 含"动手 5 分钟"章节 | 问题 4 |
| C8 篇幅下限 | 每个 lesson ≥ 150 行（低于此值告警而非报错） | 问题 7 |
| C9 文件-数据对应 | curriculum.ts 中每个 lesson.id 都有对应 `.md` 文件，反之亦然 | — |
| C10 文档同步 | CLAUDE.md 组件表与 `componentMap` 一致 | 文档漂移 |

C7、C8 在 W5/W6 完成前设为 warning 级别，完成后升为 error。

### 接入

```jsonc
// package.json
"scripts": {
  "check": "node scripts/check-curriculum.mjs",
  "build": "npm run check && tsc && vite build"
}
```

在 `.github/workflows/` 的部署流程前加一步 `pnpm check`。

### 验收标准

- 脚本在当前代码库上跑，能准确报出 W0 之前的全部问题（用 `git stash` 验证）
- W0 修完后 C1/C2/C5/C10 全绿
- CI 上 check 失败能阻断部署

---

## W2 · 补齐交互组件覆盖（问题 1）

**现状**：8 个组件、9 次调用、10/16 模块零交互。CLAUDE.md 把交互组件当核心卖点，实际兑现率 10.3%。

### T2.1 接回已实现但零调用的组件

`EmbeddingExplorer` 已实现、已在 `componentMap` 注册，但内容中零引用。接入 `src/content/module-04/lesson-l04-03.md`（Embedding 与向量检索深度）——这是全课程最需要交互演示的知识点之一。

### T2.2 补齐 CLAUDE.md 承诺过的两个组件

| 组件 | type | 接入位置 | 演示内容 |
|------|------|----------|----------|
| `ChunkingVisualizer` | `chunkingDemo` | M4-02 分块策略全解 | 同一段文本用固定/语义/层级/Agentic 四种策略分块的可视化对比，可调 chunk size 与 overlap |
| `RAGPipelineDemo` | `ragPipeline` | M4-01 或 M4-06 | 检索→重排→生成全链路，每一环可见中间产物 |

### T2.3 为 10 个零交互模块各补 1 个

按投入产出比排序，**前 4 个必做，后 6 个视精力**：

| 优先级 | 模块 | 建议组件 | type | 为什么交互能讲清楚而文字不能 |
|--------|------|----------|------|------------------------------|
| 必做 | M3 上下文工程 | TokenBudgetAllocator | `tokenBudget` | 拖动各部分占比，实时看总量与超限，比表格直观一个量级 |
| 必做 | M13 评估护栏 | EvalScorecard | `evalScorecard` | 同一批输出在不同评分维度下的结果对比，展示 LLM-as-Judge 的偏倚 |
| 必做 | M6 工具调用 | ToolCallInspector | `toolCallInspector` | 并行 vs 串行工具调用的时序对比 |
| 必做 | M11 多智能体 | MultiAgentDebate | `multiAgentDebate` | 辩论/监督/协作三种拓扑的消息流动画 |
| 选做 | M8 记忆系统 | MemoryTimeline | `memoryTimeline` | 对话推进中短期/长期记忆的写入与召回 |
| 选做 | M10 框架编排 | GraphOrchestrator | `graphOrchestrator` | LangGraph 状态机的节点流转 |
| 选做 | M14 架构设计 | ArchTradeoffMatrix | `archTradeoff` | 架构决策的权衡量化滑块 |
| 选做 | M15 生产运维 | CostLatencyTriangle | `costLatency` | 成本/延迟/质量三角的实时权衡 |
| 选做 | M16 前沿范式 | — | — | 可复用 M12 的 MultiModalDemo |
| 选做 | M4 | 已由 T2.1/T2.2 覆盖 | — | — |

### T2.4 同步文档

W0 删掉的两行加回 CLAUDE.md，并补全新增组件。此时 C10 检查应全绿。

### 验收标准

- C2/C3/C4 全绿：无孤儿组件、无悬空引用、每模块 ≥1 交互
- 每个新组件在暗/亮双主题下均正常（这是本仓库的既有约定）
- 组件不使用 localStorage 之外的浏览器存储，遵循现有 ProgressProvider 模式

---

## W3 · 模型别名层（问题 6）

**现状**：55 个文件、174 处硬编码模型标识；主力示例是已属上一代的 gpt-4o 系列；`claude-sonnet-5` 与 `claude-sonnet-4-20250514` 混用无说明。

**核心思路**：模型会一直迭代，硬编码等于给自己安排了一场每半年一次的全站 grep 替换。**加一层间接。**

### T3.1 新建 `src/data/models.ts`

```ts
export const MODELS = {
  FAST:     { alias: 'MODEL_FAST',     current: '...', role: '高频、低难度、成本敏感' },
  BALANCED: { alias: 'MODEL_BALANCED', current: '...', role: '默认主力' },
  STRONG:   { alias: 'MODEL_STRONG',   current: '...', role: '复杂推理、长上下文' },
  VISION:   { alias: 'MODEL_VISION',   current: '...', role: '多模态输入' },
  EMBED:    { alias: 'MODEL_EMBED',    current: '...', role: '向量化' },
} as const
```

同时导出白名单供 W1 的 C5 检查使用，并记录 `lastReviewed` 日期。

### T3.2 新增"模型对照表"页面

`src/pages/ModelsPage.tsx` + 路由。展示别名 → 当前实际模型 → 价格量级 → 适用场景 → 上次核对日期。**单点维护，全站受益。**

### T3.3 全站替换

写一次性脚本处理 174 处，但**必须人工复核**——有些地方模型名是在讲"选型对比"，那里的具体型号是内容的一部分，不能替换。预计需人工判断的约 30 处（集中在 M1-04 模型选型、M15-02 三角优化、M12 多模态选型）。

### T3.4 在 M1-04 补一节说明

用一个小节讲清楚"为什么课程用别名而不是具体型号"，这本身就是一堂工程课——抽象出变化点是架构的基本功。

### 验收标准

- C5 检查全绿
- 除刻意保留的选型对比段落外，正文无裸模型标识
- 模型对照表页面可访问，暗亮双主题正常

---

## W4 · 收敛语言定位（问题 3）

**现状**：392 Python vs 4 TypeScript，但 M1-03 标题写着"Python & TypeScript 双语言"，CLAUDE.md 也写着双语言。

**决策：坦白定位为 Python 主线。** 假装双语言比承认单语言伤害更大——学员冲着 TS 来，学到 M2 就会发现被骗了。而且 Agent 生态在 Python 侧确实完整得多，这个选择本身是对的，只是文案没跟上。

### 任务

- `curriculum.ts` M1-03 标题：`动手调用 LLM API（Python & TypeScript 双语言）` → `动手调用 LLM API`；在 `summary` 里说明"附 TypeScript 平行实现供前端背景的同学参考"
- 保留 M1-03 现有的 3 个 TS 代码块，作为"其他语言怎么调"的示例
- `CLAUDE.md`：`所有文本使用中文，代码示例使用 Python/TypeScript` → 明确"正文主线 Python；前端与 UI 相关内容（M17）使用 TypeScript/React"
- 课程导览页/README 加一句语言说明，让人在报名前就知道

### 验收标准

- 全站不再出现"双语言"承诺
- M17 的两节 UI 课天然补足 TypeScript 侧，形成合理分工

---

## W5 · 补练习闭环（问题 4 · 最大教学短板）

**现状**：103 个内容文件中仅 1 个含练习。整门课是纯阅读式，学员唯一的动手机会是模块末尾的大项目——中间几个小时没有任何反馈点。

### T5.1 先给渲染器加折叠能力

`src/components/MarkdownRenderer.tsx` 目前只支持 `markdown` 和 `interactive` 两种段落。新增第三种：

```
::details{summary="参考答案"}
答案正文…
::
```

解析逻辑复用现有的 `parseInteractiveDirective` 模式，渲染为原生 `<details>` 并套用现有的 `ink-*` 色阶样式。

### T5.2 定义练习标准结构

每节课末尾（"要点总结"之前）插入固定结构：

```markdown
### 动手 5 分钟

**1. 判断题** —— …（一句话陈述，问对错并说明理由）

**2. 改错题** —— 下面这段代码有一个问题，找出来并说明后果：
    ```python
    …
    ```

**3. 补全题** —— 在 X 场景下，你会怎么改造上面的实现？

::details{summary="参考答案"}
…
::
```

三道题控制在 5 分钟内完成，**不做评分系统**——提取练习的价值在于提取动作本身，不在于打分。

### T5.3 逐模块补齐 87 节

按模块顺序推进，每个模块完成后跑 `/module N review` 复核题目质量。题目要求：

- 必须基于**本节内容**可答，不能靠常识蒙
- 改错题优先取材于本节"常见陷阱"表格中的真实坑
- 避免"下列哪个不是…"这种识别题，优先用需要迁移的应用题

### T5.4 与进度系统打通

`ProgressProvider` 增加练习完成标记，LessonPage 底部的"完成并继续"按钮在练习未展开时给一个轻提示（不强制）。

### 验收标准

- C7 检查升级为 error 级别并全绿
- 抽查 10 节课的题目，均满足"不看本节内容答不出来"
- 折叠答案在暗亮双主题下渲染正常

---

## W6 · 扩写偏薄课程 + 阶段调整（问题 7、8）

### T6.1 扩写开篇课

最短的几节全部落在开篇位置，而开篇恰恰是流失率最高的地方：

| 文件 | 现状 | 目标 | 主要问题 |
|------|------|------|----------|
| `module-01/lesson-l01-01.md` | 98 行 | 250+ | 用 98 行讲完 Token、采样、上下文窗口、幻觉、涌现、训练三阶段——六个大概念每个一句带过。**这是全课程第一节，应该最厚实、最有钩子** |
| `module-07/lesson-l07-01.md` | 103 行 | 200+ | Harness 全景，缺真实故障案例 |
| `module-05/lesson-l05-01.md` | 118 行 | 200+ | Agent 定义，缺"Chatbot 做不到而 Agent 能做到"的具体对照 |
| `module-01/lesson-l01-02.md` | 121 行 | 200+ | 生成参数，可与 TemperatureSampler 组件更紧密结合 |
| `module-06/lesson-l06-04.md` | 130 行 | 200+ | MCP 入门，缺完整可跑的最小例子 |

M1-01 的具体扩写方向：把六个概念中的"幻觉"和"涌现"各扩成有实例的完整小节；开头加一个能立刻上手的钩子（比如让学员先跑一次 tokenizer 看看自己的名字被切成几个 token）；结尾明确指向 M1-02。

### T6.2 阶段划分

7 个阶段中"质量保障篇"只含 M13 一个模块，在 RoadmapPage 上与含 3 个模块的阶段并列显得突兀。两个方案：

- **方案 A（推荐）**：M7（Harness 工程化）与 M13 合并为"可靠性与质量篇"。两者主题连贯——Harness 是让它不崩，评估护栏是让它可信。代价：打乱 M5–M7 的"Agent 核心篇"，需要重新划边界
- **方案 B（低成本）**：接受不对称，在 RoadmapPage UI 上弱化阶段边框，让单模块阶段不显得空

W7 加入第八阶段（含 3 个模块）后，不对称感会进一步放大，建议倾向方案 A。**这个决策需要你拍板。**

---

## W7 · M17–M19 数据层与目录骨架

### 任务

- `curriculum.ts` 追加三个模块对象（字段按审查报告 4.3–4.5 节填写）与 P17/P18/P19
- `stages` 追加：`{ id: 8, name: '独立开发与商业化篇', range: [17, 19], color: 'from-sky-500 to-cyan-500' }`
  （配色需与现有 7 个阶段区分；`from-rose-500 to-red-500` 已被阶段 7 占用）
- 创建 `src/content/module-17/`、`module-18/`、`module-19/` 目录与空 `.md` 骨架文件（含标题与章节占位，让 C9 检查通过）
- `module-16/lesson-l16-04.md`（回顾与成长地图）末尾加一段指向第八阶段的引导
- README 数字更新为 19 模块 / 113 节 / 19 项目 / 208h

### 验收标准

- 站点能完整展示新大纲（内容页显示"内容编写中"占位）
- C1/C9 检查通过
- RoadmapPage 上第八阶段渲染正常

---

## W8 · M17 内容编写（10 节 + P17）

按 `/module 17 create` 工作流推进。**编写顺序建议 01→03→06→07→08→09→10，最后写 04/05 两节 UI 课**——UI 那两节最需要仔细打磨，放在对模块整体节奏有把握之后写。

### 重点提示

**L17-04 / L17-05 是本模块的技术密度高点。** 这两节有个天然优势：**本仓库自己就是教材**。以下都是可直接引用的真实实现，比编造示例可信得多：

- `src/index.css` 的 `:root` / `html.light` 双主题 CSS 变量体系 → 讲"主题单一真源"
- `index.html` `<head>` 里的防闪烁脚本 → 讲暗色模式的真实工程细节
- CLAUDE.md 里"不要使用 `@apply` 引用 CSS 变量颜色（Tailwind 3 会 bake 值）" → 讲"把踩过的坑固化成 AI 永不再犯的规则"，这是 L17-04 第一个技巧的完美样板
- `ink-*` / `brand-*` 色阶体系 → 讲设计 token 的语义化命名
- 亮色模式下代码高亮色需单独覆盖 → 讲双模式并行截图对比的必要性

**新增交互组件**：`UIStateMatrix`（`uiStateMatrix`）——一个组件在 loading / empty / error / partial / success 五态间切换预览，外加边界数据（0 条 / 1000 条 / 超长文本）。这是文字讲不清而交互一秒就懂的知识点，接入 L17-05。

### P17 交付物核对

重建仓库 + 生产级前端 + `CLAUDE.md` + 规格文档 + 组件库与设计 token + 视觉回归基线截图 + 对照实验报告（耗时/缺陷/Token 成本/返工次数，含"AI 在哪些环节帮了倒忙"的诚实分析）。

### 验收

`/module 17 review` 一轮 + W1 全部检查项通过。

---

## W9 · M18/M19 内容编写（12 节 + P18/P19）

严格在 W8 之后——L18-03 产出的 PRD 直接喂给 M17 的 Spec-Driven 流程，P19 以 P18 的产品为对象。

### 重点提示

**新增交互组件 `CostModel`（`costModel`）**：AI 产品单元经济学计算器。拖动用户数、人均月调用量、输入/输出 token 数、模型单价、缓存命中率，实时算出每用户成本、毛利率、盈亏平衡点。接入 L18-05。**这是整个商业化模块最适合交互演示的知识点**——"边际成本不为零"这件事，学员拖一次滑块的理解深度超过读三页文字。

**免责声明必须显式写入**：L18-05（定价与财务）和 L19-05（合规与风险）需明确声明本内容不构成法律或财务建议，涉及具体决策请咨询专业人士。这不是免责套话，是对学员负责——独立开发者最容易在税务和合规上吃暗亏。

**语气把握**：这两个模块讲的是有真实失败率的事情。要避免两个极端——既不能渲染成"人人可做的躺赚"，也不能写成劝退。诚实的基调：**大部分独立产品不赚钱，但完整跑通一遍闭环的人，即使产品失败，能力也已经不可逆地上了一个台阶。** P19 的验收底线定为"数据真实、复盘诚实"而非"必须盈利"，就是这个基调的体现。

### 验收

`/module 18 review` 与 `/module 19 review` 各一轮 + W1 全部检查项升为 error 级别后全绿。

---

## 问题覆盖对照表

确认审查报告中 9 个问题全部有归属：

| # | 问题 | 严重度 | 归属波次 | 验收方式 |
|---|------|--------|----------|----------|
| 1 | 交互组件严重欠用（9/87） | P0 | W2 | C2/C3/C4 检查 |
| 2 | 时长三套口径矛盾 | P0 | W0 | C1 检查 |
| 3 | 双语言名不副实 | P0 | W4 | 人工复核文案 |
| 4 | 无练习自测（1/103） | P1 | W5 | C7 检查 |
| 5 | `LessonType` 死定义 | P1 | W0 | TS 编译 |
| 6 | 模型老化 + 命名混乱 + 笔误 | P1 | W0 + W3 | C5 检查 |
| 7 | 篇幅方差大、开篇课偏薄 | P2 | W6 | C8 检查 |
| 8 | 阶段划分粒度不均 | P2 | W6 | 需你拍板方案 A/B |
| 9 | 商业维度完全缺失 | P2 | W7–W9 | 新模块上线 |
| — | 防回归机制缺失 | 新增 | W1 | CI 阻断 |

---

## 建议的推进节奏

**第 1 周**：W0 + W1 + W4（数据、防线、文案，共 2 天）→ 立刻获得一个数字自洽、有 CI 守护的代码库。剩余时间启动 W3。

**第 2–3 周**：W2 + W5 并行。W2 是前端组件活，W5 是内容活，互不干扰。这两周结束后，"图文 + 交互"的定位才算真正兑现。

**第 4 周**：W6 + W7。扩写偏薄课程的同时把新模块骨架立起来。W6 的阶段划分决策需要你先拍板。

**第 5–7 周**：W8 + W9。新内容编写，占整体工作量的近一半。

### 如果时间有限，最小可交付集

只做 **W0 + W1 + W7 + W8**（约 7 天）：数据自洽 + 防回归 + AI Coding 模块上线。这已经能解决可信度问题并交付你最想要的新内容。W5（练习）虽然教学价值最高，但工作量大且可以持续渐进补充，适合放在后面滚动推进。
