# 《AI Agent 大师之路》课程审查报告

> 审查日期：2026-07-27（v2：扩充 AI Coding 前端 UI 内容，商业化拆分为两个模块）
> 审查范围：16 个模块 / 91 节课 / 16 个项目 / 103 个内容文件 / 约 26,000 行 Markdown
> 审查视角：资深 AI Agent 工程与课程设计

---

## 一、总体结论

这是一套**结构完整、技术判断成熟**的课程。它最突出的优点是没有停留在"调 API 教程"的层面，而是把 Agent 当作一个**工程系统**来讲：上下文工程独立成模块、Harness 健壮性独立成模块、评估与护栏给了 16 小时、生产运维和架构决策各占一整块。这个骨架比市面上绝大多数 Agent 课程都要正确。

抽读的内容质量也在线。以 M5-02《手写一个最小 Agent Loop》为例，它先给出 while 循环的本质、再逐步实现、最后用"关键设计决策"和"常见陷阱"表格收口，甚至在 `calculate` 工具里主动提示"生产环境禁止 eval"并给了受限 AST 求值的替代实现——这是有工程洁癖的作者才会写的细节。M14-03 拆 Devin 时还专门写了一节《拆解的边界：别把推测当事实》，这种知识论上的诚实很难得。

**核心问题不在内容深度，而在三个方面：交付形态（交互组件形同虚设）、数据一致性（时长口径全线对不上）、学习闭环（几乎没有练习与反馈机制）。** 另外，课程在"技术能力"上已经修到专家级，但在"如何用这些能力换取收入"上完全空白——这正是本次要补的方向。

评级：**技术内容 A- ／ 教学设计 B- ／ 工程一致性 C+**

---

## 二、优点

**知识架构的取舍很有品味。** 把"上下文工程"（M3）从 Prompt 工程里拆出来单列，是过去两年 Agent 工程实践中最重要的认知升级之一，很多课程还在把它当成 Prompt 的附属话题。同样，M7 "Agent Harness 工程化"单独成章——重试、退避、Checkpointing、Circuit Breaker、降级链——这是把 Agent 当分布式系统看的成年人视角，而不是 demo 视角。

**模块之间有真实的引用关系。** 内容里能看到 "用 M3 的压缩策略裁剪历史"、"这对应 M5 的 Plan-and-Execute" 这样的交叉引用，说明作者是当作一个整体在写，而不是 16 篇独立文章拼起来。`prerequisites` 和 `competency` 字段在数据层也做了 86/87 处标注，覆盖率接近满分。

**每节课有稳定的收口结构。** 87 个 lesson 全部带"要点总结"，覆盖率 100%。多数课程还配有"常见陷阱"表格和"关键设计决策"问答，这是把作者的实战经验显式化的好做法。

**项目线设计合理。** 16 个项目按模块递进，从 CLI 助手到企业级 RAG 到毕业设计，每个都有明确的交付物和技术栈定义，不是"做个 demo"的空泛要求。

**内容治理有迹可循。** git log 显示每个模块开发完成后都跑过一轮 review 并提交修复（`fix(m13): 按 review 结论修复...`），这个习惯本身就是质量保障。

---

## 三、问题清单

### P0 — 影响课程可信度，建议优先修

#### 1. 交互组件严重欠用，"图文+交互"的定位没有兑现

CLAUDE.md 把交互组件列为核心卖点，实际情况是：

| 事实 | 数据 |
|------|------|
| 已实现的组件 | 8 个 |
| 内容中的实际调用 | **9 次** |
| 使用了交互组件的 lesson | **9 / 87（10.3%）** |
| 完全没有交互组件的模块 | M3、M4、M6、M8、M10、M11、M13、M14、M15、M16（**10 / 16**） |

更具体的问题：

- **`EmbeddingExplorer` 组件已实现、已在 `componentMap` 注册，但内容中零调用。** M4 是 RAG 深度实战——向量相似度检索恰恰是最需要交互演示的知识点，组件写好了却没接进去。
- **CLAUDE.md 声称存在 `chunkingDemo`（ChunkingVisualizer）和 `ragPipeline`（RAGPipelineDemo）两个组件，实际文件不存在，`componentMap` 里也没有。** 这是文档与代码的直接漂移，会误导后续开发（包括 AI 协作者）。
- git 历史里有一条 `refactor(content): 移除交互式沙箱演示组件`，说明组件接入过程中出现过反复，但没有留下决策记录。

**建议**：要么把交互组件补到每个模块至少 1 个（M4 优先接回 `EmbeddingExplorer`，并补 `chunkingDemo` / `ragPipeline`），要么下调 CLAUDE.md 和 README 的宣传口径。当前状态是两头不靠。

#### 2. 时长数据三套口径互相矛盾

| 口径 | 数值 |
|------|------|
| README 声称 | ~159 小时 |
| curriculum.ts 中 16 个模块 `hours` 之和 | **169 小时** |
| 91 节课 `duration` 之和 | **约 59 小时**（3,555 分钟） |

模块声明时长普遍是 lesson 时长合计的 **2.5–3.4 倍**：

```
M1  声明  8h  ← lesson 合计 2.4h
M13 声明 16h  ← lesson 合计 5.8h
M16 声明  8h  ← lesson 合计 2.5h
```

即使把项目实践时间算进去，也需要在数据模型里显式表达（比如给 `Project` 加 `hours` 字段，或给 `Module` 拆成 `lessonHours` / `projectHours`）。否则学员在模块页看到"16 小时"、点进去 8 节课加起来只有 5.8 小时，会直接怀疑数据是编的。

**建议**：在 `types.ts` 里给 `Project` 增加 `hours` 字段，让 `Module.hours = Σ lesson.duration + project.hours`，并在页面上分列显示"精讲 X 小时 + 实战 Y 小时"。同时统一 README。

#### 3. "Python & TypeScript 双语言"名不副实

代码块语言分布：

```
python      392
markdown     17
bash         16
yaml          7
jsx           6
typescript    4      ← 其中 3 个集中在 M1-03 一节课
json          3
```

M1-03 的课程标题就叫《动手调用 LLM API（**Python & TypeScript 双语言**）》，但整个课程 91 节里 TypeScript 代码块只有 4 个。CLAUDE.md 也写着"代码示例使用 Python/TypeScript"。对一个用 React + TypeScript 构建的站点来说，这个反差尤其刺眼。

**建议**：二选一。要么承认这是一门 Python 课程，把 M1-03 标题改成《动手调用 LLM API》并调整 CLAUDE.md；要么在关键节点（M1、M6 工具定义、M10 框架、M17 AI Coding）补 TypeScript 平行实现。**推荐前者**——假装双语言比坦白单语言伤害更大，而且 TS 生态在 Agent 领域确实不如 Python 完整。

### P1 — 影响学习效果

#### 4. 几乎没有练习、自测与形成性评估

103 个内容文件中，包含"练习/自测/思考题/作业"的只有 **1 个**（M13-07）。整门课是纯阅读式的：读正文 → 读代码 → 读要点总结 → 进入下一节。学员唯一的动手机会是模块末尾的大项目，而项目往往需要几小时到几天，中间没有任何低成本的反馈点。

这是当前课程设计上最大的短板。认知负荷理论和间隔提取（retrieval practice）的研究都指向同一个结论：**没有提取练习的阅读，留存率极低**。

**建议**：为每节课补一个轻量的"动手 5 分钟"小节——2–3 道判断/改错/补全题，或一个 20 行以内的可运行片段修改任务。不需要评分系统，给出参考答案即可（可折叠）。这个改动成本远低于再写一个模块，收益却更高。

#### 5. `LessonType` 定义了 4 种，实际只用了 3 种

`types.ts` 定义 `'理论' | '实战' | '项目' | '复盘'`，实际分布是理论 44 / 实战 39 / 复盘 4 / **项目 0**。`'项目'` 类型从未被使用——因为项目走的是独立的 `Project` 接口。死定义应该清理，否则会误导后续扩展。

#### 6. 模型版本已经开始老化，且命名不统一

| 模型标识 | 出现次数 |
|----------|----------|
| `gpt-4o-mini` | 100 |
| `gpt-4o` | 68 |
| `claude-sonnet-5` | 11 |
| `claude-sonnet-4-20250514` | 11 |
| `claude-opus-4-8` | 5 |
| `claude-haiku-4-5` | 2 |

三个问题：

- **主力示例模型是 gpt-4o 系列**，在 2026 年年中已属于上一代，学员照着跑会得到与课程描述不符的效果和价格。
- **同一套 Claude 命名并存两代**：`claude-sonnet-5` 和 `claude-sonnet-4-20250514` 混用，没有说明为什么。
- **`claude-opus-4-8` 疑似笔误**，出现在 M1-04 和 M15-02 的模型路由示例中，不是任何真实模型标识。

**建议**：抽出一个"模型别名层"——课程正文统一用 `MODEL_FAST` / `MODEL_BALANCED` / `MODEL_STRONG` 这类语义化常量，在 M1 里用一节课集中说明当前映射，并在站点上加一个"模型对照表"页面单点维护。这样模型迭代时只改一处，而不是全站 200 多处 grep 替换。

### P2 — 打磨项

#### 7. 内容篇幅方差偏大，入门课偏薄

lesson 长度从 98 行到 335 行，相差 3.4 倍。值得注意的是**最短的三节全部集中在入门/开篇位置**：M1-01（98 行）、M7-01（103 行）、M5-01（118 行）。而入门第一课恰恰是流失率最高的一节，应该是最厚实、最有钩子的。M1-01 用 98 行讲完 Token、采样、上下文窗口、幻觉、涌现能力、训练/微调/推理三阶段——每个都是一句带过，密度过高而深度不足。

#### 8. 阶段划分粒度不均

7 个阶段中，"质量保障篇"只含 M13 一个模块，而"架构设计与生产落地篇"含 M14–M16 三个。单模块阶段在路线图页面上会显得突兀。可以考虑把 M13 与 M7（Harness）合并为"可靠性与质量篇"，或接受这种不对称并在 UI 上弱化阶段边框。

#### 9. 商业与产品维度完全缺失

现有内容里 "PMF" 0 次、"获客" 0 次、"变现" 0 次、"独立开发" 0 次、"定价" 2 次、"商业化" 1 次。课程把学员一路培养到"能设计和运维生产级 Agent 系统"，然后就结束了——但对大量学习者来说，真正的目标不是进大厂当架构师，而是**用这套能力自己做出能赚钱的产品**。M14 拆了 Cursor 和 Devin 的架构，却没有一节课讲"你怎么做一个自己的"。

这正是下面第四节要补的内容。

---

## 四、新增内容规划：AI Coding + 一人公司

### 4.1 定位与差异化

需要先划清与现有内容的边界，避免重复：

| 已有 | 新增 |
|------|------|
| M14-02/03 拆 **Cursor / Copilot / Devin 的架构原理**——"它们是怎么造出来的" | M17 教 **怎么把这些工具用成自己的产能**——"我怎么用它一天干三天的活" |
| M15 讲 **企业级生产架构与 SRE**——多人团队、完整可观测性 | M18 讲 **一人可维护的规模**——如何用 1/10 的复杂度守住 90% 的可靠性 |
| M16 毕业设计 = **技术毕业**（做出一个生产级 Agent 产品） | P18 = **商业毕业**（产品上线并可收费）、P19 = **经营毕业**（让它活过 30 天） |

一句话概括新增部分的主线：**用 AI Coding 把一个想法变成系统，再把系统变成收入，全流程一个人跑完。**

### 4.2 位置：新增第八阶段「独立开发与商业化篇」（M17–M19）

```
现有：
  阶段1 筑基篇              M1–M2
  阶段2 上下文与知识篇       M3–M4
  阶段3 Agent 核心篇        M5–M7
  阶段4 记忆执行与编排篇     M8–M10
  阶段5 多智能体与多模态篇   M11–M12
  阶段6 质量保障篇          M13
  阶段7 架构设计与生产落地篇 M14–M16

新增：
  阶段8 独立开发与商业化篇   M17–M19   ← 追加在末尾
    M17 AI Coding 工程实践        —— 产能
    M18 产品化：从想法到可收费产品  —— 上线
    M19 增长、运营与可持续          —— 存活
```

**为什么拆成三个模块而不是两个：** 初稿把商业化压成一个 7 节的模块，实际盘下来"从想法到上线"和"上线之后怎么活"是两套完全不同的技能——前者是产品与工程，后者是增长与经营，中间还隔着一个绝大多数独立开发者都会踩的坑（**发布当天有流量，第二周归零**）。放在一个模块里必然有一半被压缩成标题党。拆开之后每个模块有清晰的单一身份，也各自配一个能验证的项目。

**为什么放在最后而不是插在中间：** M17 要复用 M3 的上下文工程（迁移到 codebase 场景）、M12 的多模态（截图驱动 UI 生成）、M13 的护栏思想（迁移到代码审查）、M15 的运维基线（压缩到单人规模）；M18/M19 要复用 M11 的多智能体（产品决策辩论）和 M8 的程序记忆（运营 SOP 沉淀）。放在前面会产生大量前向依赖。

**追加而非重编号的好处：** 现有 M16 的目录、lesson ID、`prerequisites` 引用全部不动，零迁移成本。代价是 M16 的《回顾与你的成长地图》不再是最后一节——建议在 M19 末尾补一节"独立开发者的成长地图"作为真正的收尾，或在 M16-04 里加一句指向 M17–M19 的引导。

**给学员的额外提示：** M17 虽然编号在 17，但它是**加速器而非终点**——建议在 M1 或课程导览页显式提示"学完 M3 之后就可以提前跳读 M17，用 AI Coding 加速后续所有模块的项目实践"。

### 4.3 M17 — AI Coding 工程实践

> **副标题**：把 Agent 能力变成你的产能——从工具选型到生产级 UI 到单人交付流水线
> **难度**：高级 ｜ **时长**：16 小时（10 节课 · 420 分钟精讲 + 项目实践）
> **图标**：⌨️ ｜ **前置**：M3（上下文工程）、M6（工具与 MCP）、M12（多模态）、M13（测试与护栏）

| 编号 | 标题 | 类型 | 时长 | 核心内容 |
|------|------|------|------|----------|
| L17-01 | AI Coding 工具全景与心智模型 | 理论 | 40min | 三代形态对比：补全型（Copilot）→ 对话型（Chat）→ Agent 型（Claude Code / Cursor Agent / Codex CLI）；各自的能力边界与失效模式；**什么任务该交给 AI、什么绝对不该**；建立"AI 是记忆力超群但没有上下文的初级工程师"的心智模型 |
| L17-02 | 为代码库做上下文工程 | 实战 | 45min | 把 M3 的方法迁移到 codebase：`CLAUDE.md` / 规则文件的分层写法（全局 → 仓库 → 目录）、仓库索引与检索、`@` 引用与文件锚定、子代理拆分、接入自建 MCP Server 打通内部系统；**上下文腐化的识别与修剪时机** |
| L17-03 | Spec-Driven 开发：让 Agent 一次做对 | 实战 | 45min | 需求 → 规格 → 实现的三段式；计划模式（plan before code）；任务拆分粒度的经验法则（"一次改动 ≤ 3 个文件"）；如何写出让 Agent 不跑偏的验收标准；对比实验：模糊指令 vs 规格化指令的产出差异 |
| **L17-04** | **AI 前端 UI 开发（上）：从设计约束到界面生成** ★新增 | 实战 | 45min | **见 4.3.1 详细展开** —— 为什么 AI 生成的 UI 总像 demo；约束前置（设计 token 写进规则文件）；组件优先于页面；截图/参考站点驱动的多模态生成；分层生成法；反"AI 味"清单 |
| **L17-05** | **AI 前端 UI 开发（下）：生产级打磨与视觉闭环** ★新增 | 实战 | 45min | **见 4.3.1 详细展开** —— 状态矩阵与边界数据；响应式的信息取舍；可访问性验收清单；**截图回喂自评 + Playwright 视觉回归**；主题与暗色模式的单一真源；前端性能预算 |
| L17-06 | Agent 化的测试、重构与调试 | 实战 | 45min | 让 AI 写测试而不是让 AI 猜；红-绿-重构循环中 Agent 的正确位置；大规模重构的分批策略与回归防护网；用 Agent 定位 bug 的提问范式；**为什么"让 AI 改到测试通过"是个陷阱**（它会改测试） |
| L17-07 | 遗留代码与大型仓库的 AI 改造 ★新增 | 实战 | 40min | Brownfield 才是常态：先让 Agent 建立仓库地图与架构摘要；渐进式绞杀者模式（strangler fig）；给没有测试的旧代码补特征测试（characterization test）作为改造安全网；跨越几十个文件的一致性修改怎么切分 |
| L17-08 | AI 生成代码的 Review 与安全基线 | 理论 | 40min | 幻觉 API 与不存在的依赖（slopsquatting 供应链攻击）、依赖投毒、密钥泄漏、许可证污染、隐蔽的并发与边界缺陷；把 M13 的护栏思想搬到代码上：**必须人审的清单 vs 可以放行的清单**；提交前的自动化闸门 |
| L17-09 | 并行 Agent：多任务并发与 worktree 工作流 ★新增 | 实战 | 40min | 单线程用 AI 是浪费：git worktree + 多会话并行推进互不冲突的任务；任务的可并行性判断；后台长任务与结果汇总；**并行的真实上限**——不是 Agent 不够，是你的 review 带宽不够 |
| L17-10 | 单人交付流水线 | 实战 | 35min | 一个人的 Git 工作流（trunk-based + 短分支）、CI 里的 AI review、自动化发布与回滚；把 M15 的运维基线压缩到"一个人周末能维护"的规模；**复杂度预算**：每引入一个组件都要问"半年后我还养得起吗" |

**P17 — AI Coding 效能对照实验**
难度：高级。用 AI Coding 全流程重建一个你已经手写过的项目（推荐用 P5 的研究 Agent 或 P9 的沙箱服务），并**给它做一个生产级的 Web 界面**，全程记录对照数据：耗时、缺陷密度、Token 成本、返工次数。
交付物：① 重建后的可运行仓库（含前端）；② 完整的 `CLAUDE.md` 与规格文档；③ 组件库与设计 token 定义；④ 视觉回归基线截图；⑤ 对照实验报告（含数据表格与"AI 在哪些环节帮了倒忙"的诚实分析）。
技术栈：Claude Code / Cursor、MCP、React + TypeScript、Playwright、pytest、GitHub Actions。

#### 4.3.1 前端 UI 两节课的详细展开

这是本次新增里技术密度最高的部分，单独展开。核心论点是：**AI 生成的界面之所以像 demo，绝大多数时候不是模型能力不够，而是你的输入里没有任何约束——你不给设计系统，它每次就自己发明一套。**

**L17-04《从设计约束到界面生成》要讲的六件事**

1. **约束前置：把设计 token 写进规则文件。** 颜色用语义层而不是十六进制（`--brand-500` 而非 `#3b82f6`）、间距用固定阶梯（4/8/12/16/24/32）、字号与行高阶梯、圆角与阴影层级、动效时长档位。本仓库的 `CLAUDE.md` 里那条"不要使用 `@apply` 引用 CSS 变量颜色（Tailwind 3 会 bake 值）"就是这类约束的正面样板——它把一个踩过的坑固化成了 AI 永远不会再犯的规则。这一节要教学员系统性地建立自己的这份清单。

2. **组件优先于页面。** 先让 AI 把 Button / Input / Card / Modal 的**全部变体和全部状态**建出来并锁定，再组装页面。反过来做（先生成页面，遇到组件就地造一个）必然导致同一个按钮在五个页面有五种样式。这是 AI UI 一致性问题的根因。

3. **视觉输入远胜文字描述。** 截图、参考站点、Figma 导出、甚至手绘草图喂给多模态模型（直接复用 M12 的能力）。"照着这张图的信息层级和留白节奏，但全部换成我们的 token" 这一句话的信息量超过一千字的文字描述。要教学员怎么组织参考图（好的参考 vs 会带偏的参考）。

4. **分层生成法。** 骨架（布局与信息层级）→ 结构（语义化 HTML）→ 样式（套 token）→ 交互（状态与动效），一层确认后再进下一层。一次性要求"给我一个漂亮的完整页面"，得到的是四不像的平庸中位数输出。

5. **反"AI 味"清单。** 紫蓝渐变背景、无意义的 emoji 当图标、居中英雄区无限堆叠、每个卡片都带阴影、圆角滥用、字重只有 bold 和 normal 两档、所有间距都是 16px。这些是模型的默认偏好，必须在规则文件里显式禁掉，否则每次都会回归。

6. **参考实现锚定。** 仓库里指定一个"标杆组件"，让 AI 照着它的写法产出新组件。这比任何抽象描述都有效——**给例子永远优于给规则**，这一点和 M2 的 Few-shot 是同一个原理，只是作用在代码层面。

**L17-05《生产级打磨与视觉闭环》要讲的七件事**

1. **状态矩阵驱动。** AI 默认只生成 happy path。必须把 loading / empty / error / partial / success 五态外加权限态显式写进验收标准，并附上边界数据：0 条、1 条、1000 条、超长不换行文本、缺图、慢网络、请求超时。**写进规格它才会生成，不写它一定不生成。**

2. **响应式不是加断点前缀。** 真正的响应式是断点处的**信息取舍**——移动端该隐藏什么、该折叠成什么、导航该变成什么形态。要求 AI 先说明取舍逻辑再写代码。附带触控目标 ≥ 44px 这类硬指标。

3. **可访问性验收清单。** 语义化标签而非 `div onClick`、键盘完全可达、focus 状态可见、对比度 ≥ 4.5:1、异步更新用 `aria-live`、尊重 `prefers-reduced-motion`。把清单给 AI 让它自查并逐条回报，比事后人工审快一个数量级。

4. **视觉回归闭环——这是全节最关键的技巧。** 两条腿：① **截图回喂自评**，把渲染结果截图交给多模态模型问"这个界面有哪些视觉缺陷、哪里对齐不一致、哪里层级混乱"——让 AI **看见**自己的输出，而不是盲写；② **Playwright 截图基线对比**，防止改 A 页面顺手弄坏 B 页面。没有这个闭环，UI 迭代到第五轮必然开始退化。

5. **主题与暗色模式的单一真源。** CSS 变量集中定义，组件只消费语义变量。亮/暗双模式并行截图对比是必做步骤——本仓库的暗色默认 + `html.light` 切换 + 防闪烁脚本就是可直接引用的现成案例，包括"亮色模式下代码高亮色需要单独覆盖"这个真实踩坑记录。

6. **前端性能预算。** AI 会毫不犹豫地 `npm install` 一个 300KB 的库来实现一个日期格式化。要设硬门槛：包体积上限、首屏指标、图片格式与懒加载策略、避免运行时 CSS-in-JS。把预算写进规则文件并在 CI 里卡住。

7. **交付前自查清单。** 一份可直接复制使用的 checklist，覆盖上述全部维度，作为学员之后每个项目的标准收口动作。

### 4.4 M18 — 产品化：从想法到可收费的产品

> **副标题**：一个人怎么把一个念头做成能收钱的东西——选题、证伪、定价、上线
> **难度**：专家 ｜ **时长**：13 小时（7 节课 · 290 分钟精讲 + 项目实践）
> **图标**：💰 ｜ **前置**：M15（生产运维）、M17（AI Coding）

| 编号 | 标题 | 类型 | 时长 | 核心内容 |
|------|------|------|------|----------|
| L18-01 | 选题与机会评估：一人可交付的边界 | 理论 | 40min | 什么规模的产品一个人做得动、什么一定做不动（需要销售团队 / 需要合规牌照 / 需要供给侧运营的直接排除）；AI 时代的细分机会图谱：被大模型顺手吃掉的 vs 反而被放大的；**"AI 套壳"的真实护城河在哪**——不在模型，在数据、工作流嵌入和分发 |
| L18-02 | 需求验证：在写第一行代码之前证伪 | 实战 | 45min | 独立开发最大的浪费是做出一个没人要的东西。落地页 + 等待列表测试、预售验证、社区信号采集与竞品评论挖掘（用 Agent 批量分析用户抱怨）；**如何设计一个能证伪你假设的实验**，而不是一个只能自我确认的实验；什么样的信号强度才值得动手 |
| L18-03 | 从头脑风暴到 PRD | 实战 | 45min | 用 Agent 做发散与收敛：合成用户访谈、需求聚类、假设排序；把 M11 的多智能体辩论范式用于产品决策（专设一个唱反调 Agent 攻击你的方案）；产出 PRD 与可执行验收标准，**直接喂给 M17 的 Spec-Driven 流程**——这是两个模块的接缝处 |
| L18-04 | 一人技术栈与复杂度预算 | 理论 | 40min | 选型第一原则：全托管优先、少即是多、无聊的技术优先；Serverless / BaaS / 托管向量库 / 托管队列的取舍；**复杂度预算**——一个人能维护的组件数量是有硬上限的，每加一个都要问"凌晨三点它挂了我修得动吗"；何时该拒绝一个"更优雅"的架构 |
| L18-05 | AI 产品的单元经济学与定价 | 理论 | 45min | 这是 AI 产品与传统 SaaS 最不一样的地方：**边际成本不为零**。每用户每月 Token 成本 → 毛利率 → 定价下限的完整推导；订阅制 / 用量制 / 混合制的适用场景；缓存、模型路由、降级对毛利的杠杆（复用 M3 Prompt Caching 与 M15 三角优化）；**免费额度的设计——它是获客成本还是无底洞** |
| L18-06 | 从 MVP 到上线：账号、订阅与计费 | 实战 | 45min | 最小可发布范围的划定（什么必须有、什么可以第二周再说）；账号体系与鉴权；订阅与用量计费接入；配额、超额与降级体验；上线检查清单（域名、隐私政策、状态页、退款流程）；灰度与首批种子用户 |
| L18-07 | 滥用防护与成本攻击防御 | 实战 | 30min | AI 产品独有的风险面：注册薅羊毛、脚本刷量、Prompt 注入诱导高成本调用、越权数据访问；速率限制与配额的分层设计；**成本告警必须早于账单**；把 M13 的护栏和 M9 的沙箱思路用在计费边界上 |

**P18 — 商业毕业：上线一个能收费的 AI Agent 产品**
难度：专家。从选题证伪到可支付上线的完整闭环。
交付物：① 公开可访问的线上产品（含账号与计费）；② 需求验证实验记录与结论；③ PRD + 架构文档；④ 单元经济学测算表（每用户成本 / 毛利 / 盈亏平衡点）；⑤ 上线检查清单执行记录。
技术栈：M17 的 AI Coding 流水线、托管 LLM API、Serverless 部署、支付服务、速率限制中间件。
验收底线：**产品上线且可完成一次真实支付**即算完成——是否拿到付费用户不作为硬性门槛。

### 4.5 M19 — 增长、运营与可持续

> **副标题**：一个人的公司怎么活下去——冷启动、增长引擎、自动化运营、长期主义
> **难度**：专家 ｜ **时长**：10 小时（5 节课 · 200 分钟精讲 + 项目实践）
> **图标**：📈 ｜ **前置**：M18（产品化）

| 编号 | 标题 | 类型 | 时长 | 核心内容 |
|------|------|------|------|----------|
| L19-01 | 冷启动：前 100 个用户从哪来 | 实战 | 40min | 渠道盘点与匹配：Product Hunt、垂直社区、Reddit/HN、开源引流、冷邮件、合作分发；每个渠道的真实转化量级与准备工作；**为什么"发一次就没了"是独立开发者最常见的死法**——发布是起点不是终点；首批用户的手动获取（do things that don't scale） |
| L19-02 | 增长引擎：用 Agent 搭建内容与 SEO 流水线 | 实战 | 45min | 把内容生产做成系统而不是苦力：选题来源 → 生成 → 人工把关 → 发布 → 效果回流的 Agent 流水线（复用 M12 多模态做配图与视频切片）；程序化 SEO 的适用条件与滥用红线；**AI 内容的质量底线**——搜索引擎和读者都在惩罚批量垃圾 |
| L19-03 | 自动化运营：一个人的公司 | 实战 | 40min | 用 Agent 做客服与工单分流、自动日报、异常归因、用户反馈聚类；**把运营 SOP 沉淀成 Agent 的程序记忆**（复用 M8 的技能库）；一人 on-call 的现实策略——哪些告警必须半夜叫醒你，哪些可以等到早上，怎么设计一个不会把你熬垮的值班规则 |
| L19-04 | 数据闭环：埋点、留存与迭代决策 | 实战 | 40min | 一人公司该埋什么、不该埋什么（贪多必烂）；激活 / 留存 / Churn 的最小指标集；留存曲线怎么读，什么形状说明 PMF 成立；**用 Agent 做定性分析**——把流失用户的原话聚类成可行动的结论；如何避免被虚荣指标骗 |
| L19-05 | 合规、风险与可持续经营 | 理论 | 35min | 数据隐私与模型服务条款、用户生成内容的责任边界、开源许可证合规；单一供应商依赖与模型下线风险的应对；基本的记账与订阅制财务概念（**明确声明：本节不构成法律或财务建议，具体决策请咨询专业人士**）；精力管理与防倦怠；**何时该体面地关掉一个产品**，以及独立开发者的长期成长地图 |

**P19 — 经营毕业：让你的产品活过 30 天**
难度：专家。以 P18 的产品为对象，运行一个完整的 30 天经营周期。
交付物：① 至少 3 个渠道的冷启动实验记录（投入、曝光、转化、单位获客成本）；② 一条跑通的自动化内容流水线（含产出物与效果数据）；③ 客服/运营自动化台（Agent 处理的工单占比）；④ 30 天指标看板（激活、留存、成本、收入）；⑤ 复盘报告——单元经济学是否成立，不成立卡在哪一环，下一步是迭代、转向还是关停。
技术栈：分析工具、Agent 内容流水线、工单系统、告警与看板。
验收底线：**数据真实、复盘诚实**。指标难看不扣分，自欺欺人才扣分。

### 4.6 变更后的课程规模

| 指标 | 现状 | 变更后 |
|------|------|--------|
| 模块 | 16 | **19** |
| 课程 | 91 | **113**（+22） |
| 项目 | 16 | **19** |
| 阶段 | 7 | **8** |
| 声明总时长 | 169h | **208h** |

新增部分的内部配比：AI Coding 10 节 / 商业化 12 节，大致 45:55。考虑到本课程学员的技术底子已经在前 16 个模块打透，商业化侧略重是合理的——那才是他们真正陌生的地形。

---

## 五、落地步骤建议

按依赖顺序，分四步走：

**第一步 · 修数据与文档（半天）**
统一 hours 口径（给 `Project` 加 `hours` 字段）、同步 README 的时长与模块数、修掉 CLAUDE.md 里不存在的 `chunkingDemo` / `ragPipeline`、清理 `LessonType` 里没用到的 `'项目'`、修正 `claude-opus-4-8` 笔误。这一步不动内容，纯粹止血。

**第二步 · 落地 M17/M18/M19 大纲（1 天）**
在 `curriculum.ts` 追加三个模块对象与 `stages` 第八阶段，创建 `src/content/module-17/`、`module-18/`、`module-19/` 目录骨架。此时站点已能展示新大纲，内容留空。

**第三步 · 补齐内容（按现有 `/module N create` 工作流）**
严格按 M17 → M18 → M19 顺序：M18 的 L18-03 产出的 PRD 直接喂给 M17 的 Spec-Driven 流程，P19 以 P18 的产品为对象，前向依赖很实。建议每个模块写完后跑一轮 `/module N review`，延续现有的质量习惯。

M17 的两节 UI 课有个天然优势——**本仓库自己就是教材**。暗色/亮色双主题、CSS 变量单一真源、防闪烁脚本、"Tailwind 3 会 bake `@apply` 变量值"这个踩坑记录、`ink-*` / `brand-*` 色阶体系，全都是可以直接拿来当案例讲的真实代码。写这两节时优先引用仓库内的实际实现，比编造示例可信得多。

**第四步 · 补交互组件与练习（可与第三步并行）**
优先级：① M4 接回 `EmbeddingExplorer`、补 `ChunkingVisualizer`；② 为新模块各配一个交互组件——`uiStateMatrix`（UI 五态矩阵切换预览，服务 L17-05）和 `costModel`（AI 产品单元经济学计算器：拖动用户数/调用量/模型单价，实时算毛利与盈亏平衡点，服务 L18-05）。这两个都是**交互能讲清楚而文字讲不清楚**的知识点，投入产出比最高；③ 全站每节课补"动手 5 分钟"练习小节。

---

## 六、一句话总结

技术骨架已经是专家水准，接下来的杠杆不在继续加深技术内容，而在**补上学习闭环（练习与交互）、修掉数据一致性问题、以及把"能造出来"延伸到"能靠它活下去"**。M17–M19 补的正是最后一块：M17 解决产能（包括绝大多数后端出身的开发者最卡的前端 UI），M18 解决从想法到收费，M19 解决活下去。这三节课加起来，才是"一个人靠 Agent 技术独立经营"这件事的完整答案。
